<div class="jumbotron">
  <div class="container">
    <h1 class="display-3">Subjects</h1>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <%= link_to("<< Back to list", subjects_path, :class => "btn btn-success") %>

      <div class="subjects new">
        <h2>Create Subject</h2>

        <%= form_for(@subject) do |f| %>

            <%= render(:partial => 'form', :locals => {:f => f}) %>

            <%= f.submit("Create Subject", class: "btn btn-primary") %>
        <% end %>

      </div>
    </div>
  </div>
</div>